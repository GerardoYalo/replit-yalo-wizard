run = "pip install -r requirements.txt -q && python main.py"
entrypoint = "main.py"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt -q && python main.py"]
deploymentTarget = "autoscale"
